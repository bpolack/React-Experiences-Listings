const { Component } = wp.element;
import './ListingGrid.css';

export class ListingGrid extends Component {
    render() {
        return (
            <div>
                Grid
            </div>
        )
    }
}

export default ListingGrid
