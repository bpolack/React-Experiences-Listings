const { Component } = wp.element;

export class ListingRows extends Component {
    render() {
        return (
            <div>
                Rows
            </div>
        )
    }
}

export default ListingRows
